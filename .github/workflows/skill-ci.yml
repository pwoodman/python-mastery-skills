name: Skill CI

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate skill references and bundle
        shell: pwsh
        run: ./scripts/validate-python-mastery-skill.ps1
